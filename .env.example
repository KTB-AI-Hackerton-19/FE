# 백엔드 배포 주소. CORS 가 열려 있어 브라우저에서 직접 호출한다.
NEXT_PUBLIC_API_BASE_URL=https://api.giftie.site

# 위 값을 비우면 같은 오리진(Next rewrite 프록시)으로 폴백한다. 그때의 프록시 대상 (서버 전용).
API_PROXY_TARGET=https://api.giftie.site

# 백엔드에서 만들어준 데모 계정 — ID: demo / PW: demo1234
# 환경변수 테스트
